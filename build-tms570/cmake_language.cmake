#### AUTOGENERATED, DO NOT EDIT ####
macro(cmake_language ACTION FUNCTION_NAME)
    if (NOT "${ACTION}" STREQUAL "CALL")
        message(FATAL_ERROR "Cannot use ${ACTION} with faux cmake_language")
    elseif (NOT COMMAND "${FUNCTION_NAME}")
        message(FATAL_ERROR "Unknown function ${FUNCTION_NAME} supplied to faux cmake_language")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 2)
        build_add_global_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 3)
        build_add_global_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 4)
        build_add_global_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 5)
        build_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 6)
        build_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target AND ${ARGC} EQUAL 7)
        build_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_global_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 2)
        build_add_module_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 3)
        build_add_module_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 4)
        build_add_module_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 5)
        build_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 6)
        build_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target AND ${ARGC} EQUAL 7)
        build_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_module_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 2)
        build_add_deployment_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 3)
        build_add_deployment_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 4)
        build_add_deployment_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 5)
        build_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 6)
        build_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target AND ${ARGC} EQUAL 7)
        build_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL build_add_deployment_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 2)
        version_add_global_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 3)
        version_add_global_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 4)
        version_add_global_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 5)
        version_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 6)
        version_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target AND ${ARGC} EQUAL 7)
        version_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_global_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 2)
        version_add_module_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 3)
        version_add_module_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 4)
        version_add_module_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 5)
        version_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 6)
        version_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target AND ${ARGC} EQUAL 7)
        version_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_module_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 2)
        version_add_deployment_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 3)
        version_add_deployment_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 4)
        version_add_deployment_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 5)
        version_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 6)
        version_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target AND ${ARGC} EQUAL 7)
        version_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL version_add_deployment_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 2)
        install_add_global_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 3)
        install_add_global_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 4)
        install_add_global_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 5)
        install_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 6)
        install_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target AND ${ARGC} EQUAL 7)
        install_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_global_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 2)
        install_add_module_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 3)
        install_add_module_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 4)
        install_add_module_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 5)
        install_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 6)
        install_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target AND ${ARGC} EQUAL 7)
        install_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_module_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 2)
        install_add_deployment_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 3)
        install_add_deployment_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 4)
        install_add_deployment_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 5)
        install_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 6)
        install_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target AND ${ARGC} EQUAL 7)
        install_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL install_add_deployment_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 2)
        sbom_add_global_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 3)
        sbom_add_global_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 4)
        sbom_add_global_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 5)
        sbom_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 6)
        sbom_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target AND ${ARGC} EQUAL 7)
        sbom_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_global_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 2)
        sbom_add_module_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 3)
        sbom_add_module_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 4)
        sbom_add_module_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 5)
        sbom_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 6)
        sbom_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target AND ${ARGC} EQUAL 7)
        sbom_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_module_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 2)
        sbom_add_deployment_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 3)
        sbom_add_deployment_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 4)
        sbom_add_deployment_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 5)
        sbom_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 6)
        sbom_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target AND ${ARGC} EQUAL 7)
        sbom_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL sbom_add_deployment_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 2)
        refresh_cache_add_global_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 3)
        refresh_cache_add_global_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 4)
        refresh_cache_add_global_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 5)
        refresh_cache_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 6)
        refresh_cache_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target AND ${ARGC} EQUAL 7)
        refresh_cache_add_global_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_global_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 2)
        refresh_cache_add_module_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 3)
        refresh_cache_add_module_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 4)
        refresh_cache_add_module_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 5)
        refresh_cache_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 6)
        refresh_cache_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target AND ${ARGC} EQUAL 7)
        refresh_cache_add_module_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_module_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 2)
        refresh_cache_add_deployment_target( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 3)
        refresh_cache_add_deployment_target( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 4)
        refresh_cache_add_deployment_target( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 5)
        refresh_cache_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 6)
        refresh_cache_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target AND ${ARGC} EQUAL 7)
        refresh_cache_add_deployment_target( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL refresh_cache_add_deployment_target)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 2)
        fpp_is_supported( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 3)
        fpp_is_supported( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 4)
        fpp_is_supported( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 5)
        fpp_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 6)
        fpp_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported AND ${ARGC} EQUAL 7)
        fpp_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_is_supported)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 2)
        fpp_setup_autocode( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 3)
        fpp_setup_autocode( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 4)
        fpp_setup_autocode( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 5)
        fpp_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 6)
        fpp_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode AND ${ARGC} EQUAL 7)
        fpp_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_setup_autocode)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 2)
        fpp_get_generated_files( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 3)
        fpp_get_generated_files( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 4)
        fpp_get_generated_files( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 5)
        fpp_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 6)
        fpp_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files AND ${ARGC} EQUAL 7)
        fpp_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_generated_files)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 2)
        fpp_get_dependencies( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 3)
        fpp_get_dependencies( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 4)
        fpp_get_dependencies( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 5)
        fpp_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 6)
        fpp_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies AND ${ARGC} EQUAL 7)
        fpp_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL fpp_get_dependencies)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 2)
        ai_xml_is_supported( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 3)
        ai_xml_is_supported( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 4)
        ai_xml_is_supported( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 5)
        ai_xml_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 6)
        ai_xml_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported AND ${ARGC} EQUAL 7)
        ai_xml_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_is_supported)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 2)
        ai_xml_setup_autocode( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 3)
        ai_xml_setup_autocode( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 4)
        ai_xml_setup_autocode( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 5)
        ai_xml_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 6)
        ai_xml_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode AND ${ARGC} EQUAL 7)
        ai_xml_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_setup_autocode)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 2)
        ai_xml_get_generated_files( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 3)
        ai_xml_get_generated_files( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 4)
        ai_xml_get_generated_files( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 5)
        ai_xml_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 6)
        ai_xml_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files AND ${ARGC} EQUAL 7)
        ai_xml_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_generated_files)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 2)
        ai_xml_get_dependencies( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 3)
        ai_xml_get_dependencies( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 4)
        ai_xml_get_dependencies( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 5)
        ai_xml_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 6)
        ai_xml_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies AND ${ARGC} EQUAL 7)
        ai_xml_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL ai_xml_get_dependencies)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 2)
        packets_is_supported( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 3)
        packets_is_supported( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 4)
        packets_is_supported( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 5)
        packets_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 6)
        packets_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported AND ${ARGC} EQUAL 7)
        packets_is_supported( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_is_supported)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 2)
        packets_setup_autocode( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 3)
        packets_setup_autocode( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 4)
        packets_setup_autocode( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 5)
        packets_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 6)
        packets_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode AND ${ARGC} EQUAL 7)
        packets_setup_autocode( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_setup_autocode)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 2)
        packets_get_generated_files( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 3)
        packets_get_generated_files( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 4)
        packets_get_generated_files( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 5)
        packets_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 6)
        packets_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files AND ${ARGC} EQUAL 7)
        packets_get_generated_files( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_generated_files)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 2)
        packets_get_dependencies( "${ARGV2}" "${ARGV1}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 3)
        packets_get_dependencies( "${ARGV2}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 4)
        packets_get_dependencies( "${ARGV2}" "${ARGV3}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 5)
        packets_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 6)
        packets_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies AND ${ARGC} EQUAL 7)
        packets_get_dependencies( "${ARGV2}" "${ARGV3}" "${ARGV4}" "${ARGV5}" "${ARGV6}")
    elseif ("${FUNCTION_NAME}" STREQUAL packets_get_dependencies)
        message(FATAL_ERROR "Faux cmake_language called with too-many arguments: ${ARGC}")
    endif()
endmacro(cmake_language)
